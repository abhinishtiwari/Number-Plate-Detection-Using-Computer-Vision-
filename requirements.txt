# Runtime dependencies live in backend/requirements.txt; this file adds the
# tooling needed to run the test suite.
#
#   pip install -r requirements.txt
-r backend/requirements.txt

pytest==9.0.1
httpx==0.28.1
